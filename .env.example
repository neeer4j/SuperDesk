# SuperDesk environment configuration template
# Copy this file to .env (local) or add the variables to your hosting provider.

# --- Shared URLs ---
# Public URL of the deployed signaling server
REACT_APP_SERVER_URL=https://superdesk-7m7f.onrender.com
# Public URL of the client frontend (used for CORS, optional for local dev)
REACT_APP_CLIENT_URL=https://your-client-domain.example.com

# --- Server configuration ---
NODE_ENV=production
PORT=3001
CLIENT_URL=https://your-client-domain.example.com

# --- TURN / STUN configuration ---
# Comma-separated list of TURN server URLs. Include both UDP (3478) and TLS (5349/443) endpoints when possible.
# Example for Twilio: turn:global.turn.twilio.com:3478?transport=udp,turns:global.turn.twilio.com:5349?transport=tcp
# Example for Cloudflare TURN Server (static credentials):
# TURN_URLS=turn:turn.cloudflare.com:3478,turn:turn.cloudflare.com:53,turn:turn.cloudflare.com:80,turns:turn.cloudflare.com:5349,turns:turn.cloudflare.com:443
TURN_URLS=turn:turn1.example.com:3478,turns:turn1.example.com:5349
# Credentials issued by your TURN provider
TURN_USERNAME=your-turn-username
TURN_CREDENTIAL=your-turn-password

# --- Cloudflare Realtime (preferred) ---
# Use these if you want the server to mint short-lived TURN credentials from Cloudflare automatically.
# Leave blank to skip (static TURN_URLS above or fallback public TURN will be used instead).
CLOUDFLARE_ACCOUNT_ID=4b7f521d06b0ab6cdbfa29649db2d650
CLOUDFLARE_API_TOKEN=your-cloudflare-realtime-api-token
# Optional: Cloudflare RTC TURN Key credentials (use these when using https://rtc.live.cloudflare.com/v1/turn)
CLOUDFLARE_TURN_KEY_ID=your-turn-key-id

CLOUDFLARE_TURN_KEY_API_TOKEN=your-turn-key-api-token



# --- Optional: fallback to public TURN (leave empty when using your own TURN) ---
# When TURN_URLS is empty or credentials are missing, the app will fall back to the public OpenRelay service.
